[package]
name = "fleurs_tools"
version = "0.2.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
mlua = { version = "0.10.3", features = ["lua53", "vendored"] }

[profile.release]
lto = false
strip = false
panic = "abort"
